import { PageHead, Card, CardHead, Badge } from "@/components/ui";
import { ScheduleAssignmentForm, ApiHint } from "@/components/admin-forms";
const shifts=[['Regular','08:00–17:00','10m grace'],['Morning','06:00–14:00','5m grace'],['Evening','14:00–22:00','5m grace'],['Night','22:00–06:00','10m grace']];
export default function Page(){return <><PageHead title="Schedule" description="Shift template, rotating schedule, split shift, field assignment, WFH, night shift, dan swap."/><div className="grid two"><Card><CardHead title="Assign schedule" subtitle="Upsert per employee + work date"/><div className="cardpad"><ScheduleAssignmentForm/><ApiHint>Night shift lintas tengah malam diproses sebagai satu work date oleh attendance engine.</ApiHint></div></Card><Card><CardHead title="Shift templates" subtitle="Contoh policy"/><div className="cardpad list">{shifts.map(s=><div className="listrow" key={s[0]}><div><strong>{s[0]}</strong><span>{s[1]} · {s[2]}</span></div><Badge>Template</Badge></div>)}</div></Card></div></>}
